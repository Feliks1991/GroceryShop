import { CartProductsCards } from "@/features";
import { FullScreenWrapper } from "@/shared";

export const CartPage = () => {
  return (
    <FullScreenWrapper>
      <CartProductsCards />
    </FullScreenWrapper>
  );
};
